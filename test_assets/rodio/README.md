These files are test fixtures copied from `rodio-reference/assets`.

They are intentionally vendored here so CI does not depend on a local ignored
`rodio-reference` checkout.

Only files referenced by `decoder_bridge_test.mbt` are included.
